language: node_js
node_js:
  - stable
install:
  - npm install
  - npm install -g codecov
  - npm install -g istanbul
script:
  - npm test
  - istanbul cover ./node_modules/mocha/bin/_mocha --reporter lcovonly -- -R test
after_success:
  - codecov
